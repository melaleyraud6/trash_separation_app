

<button class = "btn-item">
  <a href = "/" class = "back-btn">TOPページへ戻る</a>
 </button>
  <div class = "header"></div>

<%= form_with(url: search_trashes_path, remote: true, method: :get, class: "search-form") do |form| %>
  <%= form.text_field :keyword, placeholder: "品目
  を検索する", class: "search-input" %>
  <%= form.submit "検索", class: "search-btn" %>
<% end %>

  <%= render partial: "trash_list" %>

   